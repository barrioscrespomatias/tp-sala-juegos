import { RemoverCaracteresEspecialesPipe } from './remover-caracteres-especiales.pipe';

describe('RemoverCaracteresEspecialesPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoverCaracteresEspecialesPipe();
    expect(pipe).toBeTruthy();
  });
});
