import { RandomOrderDirective } from './random-order.directive';

describe('RandomOrderDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomOrderDirective();
    expect(directive).toBeTruthy();
  });
});
