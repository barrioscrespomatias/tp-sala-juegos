import { CapturarLetraTecladoDirective } from './capturar-letra-teclado.directive';

describe('CapturarLetraTecladoDirective', () => {
  it('should create an instance', () => {
    const directive = new CapturarLetraTecladoDirective();
    expect(directive).toBeTruthy();
  });
});
